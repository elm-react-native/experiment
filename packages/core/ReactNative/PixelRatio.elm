module ReactNative.PixelRatio exposing (get, getFontScale, getPixelSizeForLayoutSize, roundToNearestPixel)


get : () -> Float
get () =
    0


getFontScale : () -> Float
getFontScale () =
    0


getPixelSizeForLayoutSize : Float -> Float
getPixelSizeForLayoutSize layoutSize =
    layoutSize


roundToNearestPixel : Float -> Float
roundToNearestPixel layoutSize =
    layoutSize
